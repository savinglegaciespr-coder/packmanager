{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "P=${PORT:-3000}; [ \"$P\" = \"null\" ] && P=3000; serve -s /app/build -l $P",
    "healthcheckPath": "/"
  }
}
